precision mediump float;

attribute vec3 POSITION;

uniform mat4 WORLDVIEWPROJECTION;

varying vec4 v_Color;

void main() {
    vec4 pos = WORLDVIEWPROJECTION * vec4(POSITION, 1.0);

    pos.xy += normalize(pos.xy) * 0.0008;

    gl_Position = pos;

    v_Color = vec4(1.0);
}