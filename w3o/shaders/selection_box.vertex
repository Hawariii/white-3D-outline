precision mediump float;

attribute vec3 POSITION;

uniform mat4 WORLDVIEWPROJECTION;

varying vec4 v_Color;

void main()
{
    gl_Position = WORLDVIEWPROJECTION * vec4(POSITION, 1.0);

    // Ungu
    v_Color = vec4(0.7, 0.2, 1.0, 1.0);
}